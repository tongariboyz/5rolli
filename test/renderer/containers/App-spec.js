import assert from 'power-assert';
import App from '../../../src/js/renderer/containers/App';


/** @test {App} */
describe('renderer/containers/App', () => {
  it('is defined', () => {
    assert(App);
  });
});
